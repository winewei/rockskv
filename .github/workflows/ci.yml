name: CI

on:
  push:
    branches: [ main, master, 'claude/*' ]
  pull_request:
    branches: [ main, master ]

env:
  GO_VERSION: '1.21'
  ROCKSDB_VERSION: '10.7.5'

jobs:
  # Build and test without CGO (stub implementation)
  build-nocgo:
    name: Build without CGO
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ env.GO_VERSION }}
        cache: true

    - name: Download Go modules
      run: go mod download

    - name: Verify dependencies
      run: go mod verify

    - name: Build without CGO
      run: |
        CGO_ENABLED=0 go build -v ./...

    - name: Run tests without CGO
      run: |
        CGO_ENABLED=0 go test -v ./...

  # Build and test with CGO and RocksDB
  build-cgo:
    name: Build with CGO (RocksDB)
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ env.GO_VERSION }}
        cache: true

    - name: Install build dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          libgflags-dev \
          libsnappy-dev \
          zlib1g-dev \
          libbz2-dev \
          liblz4-dev \
          libzstd-dev

    - name: Cache RocksDB
      id: cache-rocksdb
      uses: actions/cache@v4
      with:
        path: ~/rocksdb-${{ env.ROCKSDB_VERSION }}
        key: rocksdb-${{ env.ROCKSDB_VERSION }}-ubuntu-24.04-v1

    - name: Build RocksDB from source
      if: steps.cache-rocksdb.outputs.cache-hit != 'true'
      run: |
        cd ~
        git clone --depth 1 --branch v${{ env.ROCKSDB_VERSION }} https://github.com/facebook/rocksdb.git rocksdb-${{ env.ROCKSDB_VERSION }}
        cd rocksdb-${{ env.ROCKSDB_VERSION }}
        make shared_lib -j$(nproc)

    - name: Install RocksDB
      run: |
        cd ~/rocksdb-${{ env.ROCKSDB_VERSION }}
        sudo make install-shared INSTALL_PATH=/usr/local
        sudo ldconfig

    - name: Download Go modules
      run: go mod download

    - name: Verify dependencies
      run: go mod verify

    - name: Build with CGO
      run: |
        CGO_ENABLED=1 go build -v ./...

    - name: Run tests with CGO
      run: |
        CGO_ENABLED=1 go test -v -race -coverprofile=coverage.out ./...

    - name: Upload coverage
      uses: codecov/codecov-action@v4
      with:
        files: ./coverage.out
        fail_ci_if_error: false
      continue-on-error: true

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ env.GO_VERSION }}
        cache: true

    - name: Run golangci-lint
      uses: golangci/golangci-lint-action@v7
      env:
        CGO_ENABLED: '0'
      with:
        version: v2.0.2
        args: --timeout=5m --build-tags=nocgo

  build-linux-binaries:
    name: Build Linux Binaries
    runs-on: ubuntu-latest
    needs: [build-nocgo, build-cgo, lint]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ env.GO_VERSION }}
        cache: true

    - name: Build Linux binaries
      run: |
        make build-linux-amd64
        make build-linux-arm64

    - name: Create archives
      run: |
        cd bin
        tar -czvf rockskv-linux-amd64.tar.gz -C linux-amd64 .
        tar -czvf rockskv-linux-arm64.tar.gz -C linux-arm64 .

    - name: Upload Linux AMD64
      uses: actions/upload-artifact@v4
      with:
        name: rockskv-linux-amd64
        path: bin/rockskv-linux-amd64.tar.gz
        retention-days: 30

    - name: Upload Linux ARM64
      uses: actions/upload-artifact@v4
      with:
        name: rockskv-linux-arm64
        path: bin/rockskv-linux-arm64.tar.gz
        retention-days: 30

  build-darwin-binaries:
    name: Build Darwin Binaries (Signed)
    runs-on: macos-latest
    needs: [build-nocgo, build-cgo, lint]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ env.GO_VERSION }}
        cache: true

    - name: Build and sign Darwin binaries
      run: |
        make build-darwin-amd64
        make build-darwin-arm64

    - name: Create archives
      run: |
        cd bin
        tar -czvf rockskv-darwin-amd64.tar.gz -C darwin-amd64 .
        tar -czvf rockskv-darwin-arm64.tar.gz -C darwin-arm64 .

    - name: Upload Darwin AMD64
      uses: actions/upload-artifact@v4
      with:
        name: rockskv-darwin-amd64
        path: bin/rockskv-darwin-amd64.tar.gz
        retention-days: 30

    - name: Upload Darwin ARM64 (Apple Silicon)
      uses: actions/upload-artifact@v4
      with:
        name: rockskv-darwin-arm64
        path: bin/rockskv-darwin-arm64.tar.gz
        retention-days: 30
